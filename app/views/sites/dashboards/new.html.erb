<% content_for :title, "New dashboard · #{@site.domain}" %>
<div class="shell max-w-2xl">
  <div class="pt-5 rule-heavy mb-8">
    <h1 class="text-2xl font-semibold mt-5">New dashboard</h1>
    <p class="text-sm text-ink-2 mt-2">Name it after the question it answers. You build it on the dashboard itself.</p>
  </div>
  <%= render "form" %>
</div>
