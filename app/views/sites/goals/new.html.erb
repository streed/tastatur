<% content_for :title, "New goal · #{@site.domain}" %>
<div class="shell max-w-2xl">
  <div class="pt-5 rule-heavy mb-8">
    <h1 class="text-2xl font-semibold mt-5">New goal</h1>
    <p class="text-sm text-ink-2 mt-2">Goals are matched retroactively — this will count conversions already in your history.</p>
  </div>
  <%= render "form" %>
</div>
