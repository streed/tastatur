<%# The options behind every picker on this page, and the sentence explaining
    where they came from — the pair, for a form that wants both together. A
    custom dashboard needs them in different places and renders the two halves
    itself. -%>
<%= render "sites/known_values_payload", known_values: known_values %>
<%= render "sites/known_values_note", known_values: known_values %>
