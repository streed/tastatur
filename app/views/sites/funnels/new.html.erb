<% content_for :title, "New funnel · #{@site.domain}" %>
<div class="shell max-w-2xl">
  <div class="pt-5 rule-heavy mb-8">
    <h1 class="text-2xl font-semibold mt-5">New funnel</h1>
    <p class="text-sm text-ink-2 mt-2">Funnels are computed from history, so this reports on data you already have.</p>
  </div>
  <%= render "form" %>
</div>
